<VirtualHost *:80>
    # Get the server name from the environment
    ServerName TOREPLACE
    DocumentRoot /var/www/html

    <Directory /var/www/html/>
        Options Includes FollowSymLinks
        Require all granted
        AllowOverride All
    </Directory>

</VirtualHost>